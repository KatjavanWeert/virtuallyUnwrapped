import { FontAwesomeIcon } from '@fortawesome/react-fontawesome'

export default function IconButtonRound({icon}) { //title = properties
  return <FontAwesomeIcon className="text-white" icon={icon} transform="grow-30 down-14" />
}
